import { ProcedimentoAplicado } from './procedimento-aplicado.model';

describe('ProcedimentoAplicado', () => {
  it('should create an instance', () => {
    expect(new ProcedimentoAplicado()).toBeTruthy();
  });
});
