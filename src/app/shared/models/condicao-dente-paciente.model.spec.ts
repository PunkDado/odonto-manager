import { CondicaoDentePaciente } from './condicao-dente-paciente.model';

describe('CondicaoDentePaciente', () => {
  it('should create an instance', () => {
    expect(new CondicaoDentePaciente()).toBeTruthy();
  });
});
