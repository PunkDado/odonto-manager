import { ContaBancariaDentista } from './conta-bancaria-dentista.model';

describe('ContaBancariaDentista', () => {
  it('should create an instance', () => {
    expect(new ContaBancariaDentista()).toBeTruthy();
  });
});
